# Цепочка инлайн-режима для файловой библиотеки

## Как работает цепочка

### 1️⃣ Главное меню
**Запрос:** `@AI_BOT_LDV_bot файлы` или `@AI_BOT_LDV_bot files`

Показывает две плашки:
- **➕ Добавить** - с 4 switch_inline кнопками под сообщением
- **🗑 Удалить** - с 4 switch_inline кнопками под сообщением

### 2️⃣ Выбор типа файла (через switch_inline)

**Для добавления:**
- 📄 Пасты → `add_paste`
- 🎙 Голосовые → `add_voice`
- 📹 Медиа → `add_video`
- 💟 Стикеры → `add_sticker`

**Для удаления:**
- 📄 Пасты → `del_paste`
- 🎙 Голосовые → `del_voice`
- 📹 Медиа → `del_video`
- 💟 Стикеры → `del_sticker`

### 3️⃣ Действие

**При добавлении (`add_*`):**
- Показывается плашка "➕ Добавить [тип]"
- При выборе плашки запускается процесс добавления в ЛС бота
- Бот отправляет инструкции для загрузки файла

**При удалении (`del_*`):**
- Показывается список существующих файлов этого типа
- Каждый файл - это плашка с возможностью удаления
- Используется существующая логика `_build_library_file_results()` с `mode="delete"`

## Технические детали

### Изменения в коде:

1. **`_build_files_main_menu()`** - добавлены switch_inline кнопки в плашки
   - Используется `Button.switch_inline(..., same_peer=True)`
   - 2x2 сетка кнопок для каждой плашки

2. **`on_inline_query()`** - обработка запросов `add_*` и `del_*`
   - Для добавления: показ плашки запуска процесса
   - Для удаления: показ списка файлов

3. **`_build_add_file_results()`** - новая функция
   - Создает плашку для запуска процесса добавления
   - ID формата `add_start:{file_type}`

4. **`_handle_reply_inline_send()`** - обработка выбора плашки
   - Перехватывает `add_start:*` результаты
   - Запускает процесс добавления файла через `pending[admin_id]`
   - Отправляет prompt из `FILE_TYPE_ADD_PROMPTS`

## Преимущества

✅ **Seamless переходы** - через switch_inline без обычных кнопок
✅ **Мгновенный отклик** - как в топовых ботах (@gif, @pic)
✅ **Не ломает существующий функционал** - поиск по библиотеке работает как раньше
✅ **Красивый UX** - минимум текста, максимум функционала

## Пример использования

```
1. Пользователь: @AI_BOT_LDV_bot файлы
   → Показ: [➕ Добавить] [🗑 Удалить]

2. Выбирает: ➕ Добавить
   → Вставляется: "Выберите тип файлов для добавления:"
   → Под сообщением: [📄 Пасты] [🎙 Голосовые] [📹 Медиа] [💟 Стикеры]

3. Нажимает: 📄 Пасты
   → Открывается inline: @AI_BOT_LDV_bot add_paste
   → Показ: [➕ Добавить пасты]

4. Выбирает плашку
   → В ЛС бота: "Введите название пасты:"
   → Процесс добавления запущен
```


